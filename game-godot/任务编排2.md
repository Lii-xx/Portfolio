# Godot 卡牌游戏 - 美术开发任务编排 v2（剩余任务）

> **用途**：本文档是 `任务编排.md` 的续篇，聚焦**剩余未完成任务**的执行编排。
> 新对话的 AI 应先读 `PROJECT_STATUS.md` 和 `美术开发大纲2.md`，再读本编排。
> 项目路径：`d:\Desktop\求职\个人网站开发\game-godot`
> **核心原则**：AI 负责代码，用户负责素材。已完成任务（T1/T3/T5/T6/Bug1）不要重做。

---

## 一、剩余任务总览

| 任务 | 名称 | AI 可独立完成 | 需用户素材 | 工作量 | 依赖 |
|------|------|---------------|------------|--------|------|
| T0 | 素材准备 | - | 全部 | - | 无（用户做）|
| T4 | 粒子效果 P1 | ✅ | - | 中 | T3（已完成）|
| T8 | 背景图代码支持 | ✅ | - | 轻 | 无 |
| T10 | 氛围动效 P2 | ✅ | - | 中 | T3（已完成）|
| Bug2 | F11 全屏修复 | ✅ | - | 轻 | 无 |
| T2 | 音效素材接入 | ✅ | SFX 15 + BGM 5 | 轻 | T0 + T1（已完成）|
| T7 | 卡牌图素材接入 | ✅ | 卡牌图 20 | 轻 | T0 + T6（已完成）|
| T9 | 背景图素材接入 | ✅ | 背景图 2 | 轻 | T0 + T8 |
| T11 | 最终整合测试 | ✅ | - | 轻 | 全部 |

**执行顺序**：
```
用户准备素材(T0) ──┐
                   ├─→ T2（音效接入，需素材）
                   ├─→ T7（卡牌图接入，需素材）
                   └─→ T9（背景图接入，需素材 + T8）

AI 纯代码线（串行，都改 battle_screen.gd）：
T4（粒子）→ T8（背景图代码）→ T10（氛围动效）→ Bug2（F11）
                                                              └─→ T11（整合）
```

**关键约束**：T4/T8/T10 都改 `battle_screen.gd`，**必须串行**，不可并行。

---

## 二、T0：素材准备清单（用户做，AI 不参与）

> AI 无法下载素材，需要用户提前准备。以下清单可分批提供给 AI。

### 2.1 必备素材

#### 音频素材（放 `game-godot/assets/audio/`）

**SFX 短音效 15 个**（`.wav` 格式，放 `sfx/` 目录）：

| 文件名 | 用途 | 获取建议 |
|--------|------|----------|
| ui_click.wav | UI点击 | Kenney "Interface Sounds" |
| card_play.wav | 出牌 | Kenney "Interface Sounds" |
| card_draw.wav | 抽牌 | Kenney "Interface Sounds" |
| attack_hit.wav | 攻击命中 | Kenney "Impact Sounds" |
| attack_heavy.wav | 重击/AOE | Kenney "Impact Sounds" |
| block.wav | 格挡 | Kenney "Impact Sounds" |
| heal.wav | 治疗 | Freesound 搜 "heal" / "sparkle" |
| monster_die.wav | 怪物死亡 | Freesound 搜 "monster death" |
| player_hurt.wav | 玩家受伤 | Freesound 搜 "hurt" / "grunt" |
| dice_roll.wav | 骰子滚动 | Freesound 搜 "dice" |
| buff.wav | 增益 | Freesound 搜 "buff" / "power up" |
| debuff.wav | 减益 | Freesound 搜 "debuff" / "curse" |
| poison.wav | 中毒 | Freesound 搜 "poison" / "bubble" |
| fire.wav | 火焰 | Freesound 搜 "fire" / "flame" |
| level_up.wav | 升级 | Freesound 搜 "level up" / "fanfare" |

**BGM 背景音乐 5 首**（`.ogg` 格式，放 `bgm/` 目录）：

| 文件名 | 风格 | 获取建议 |
|--------|------|----------|
| title.ogg | 标题页，神秘悠扬 | Pixabay 搜 "fantasy ambient" |
| battle.ogg | 普通战斗，紧张节奏 | Pixabay 搜 "rpg battle" |
| boss.ogg | BOSS战，史诗感 | Pixabay 搜 "epic battle" |
| campfire.ogg | 火堆，温暖宁静 | Pixabay 搜 "calm campfire" |
| victory.ogg | 通关，欢快 | Pixabay 搜 "victory fanfare" |

#### 卡牌图 20 张（`.png` 透明背景，放 `game-godot/assets/cards/`）

文件名必须与 `data/cards/` 下 JSON 文件名对应：
```
blank.png         bloodthirst.png   cleave.png        combo_slash.png
defend.png        dice.png          dual.png          fate_wheel.png
iron_shield.png   iron_wall.png     lucky_draw.png    poison_spread.png
replay.png        shield_counter.png show_fangs.png   spin.png
strike.png        thorn_armor.png   vampiric.png      war_cry.png
```
- 尺寸：建议 128×128 或 96×60
- 获取：Leonardo.ai（免费额度）/ Stable Diffusion / Kenney

#### 背景图 2 张（`.png` 或 `.jpg`，放 `game-godot/assets/backgrounds/`）

| 文件名 | 用途 | 尺寸 |
|--------|------|------|
| boss_bg.png | BOSS战背景 | 1280×720 |
| campfire_bg.png | 火堆背景 | 1280×720 |

（`battle_bg.png` 已存在，是默认背景，无需准备）
- 获取：Pixabay / Unsplash / AI 生成

### 2.2 素材准备进度跟踪

用户准备素材时，可按此清单勾选：

```
SFX（15个）：
[ ] ui_click.wav    [ ] card_play.wav   [ ] card_draw.wav
[ ] attack_hit.wav  [ ] attack_heavy.wav [ ] block.wav
[ ] heal.wav        [ ] monster_die.wav [ ] player_hurt.wav
[ ] dice_roll.wav   [ ] buff.wav        [ ] debuff.wav
[ ] poison.wav      [ ] fire.wav        [ ] level_up.wav

BGM（5首）：
[ ] title.ogg       [ ] battle.ogg      [ ] boss.ogg
[ ] campfire.ogg    [ ] victory.ogg

卡牌图（20张）：
[ ] strike.png      [ ] defend.png      [ ] cleave.png
[ ] combo_slash.png [ ] dice.png        [ ] dual.png
[ ] fate_wheel.png  [ ] iron_shield.png [ ] iron_wall.png
[ ] lucky_draw.png  [ ] poison_spread.png [ ] replay.png
[ ] shield_counter.png [ ] show_fangs.png [ ] spin.png
[ ] thorn_armor.png [ ] vampiric.png    [ ] war_cry.png
[ ] bloodthirst.png [ ] blank.png

背景图（2张）：
[ ] boss_bg.png     [ ] campfire_bg.png
```

### 2.3 素材获取网站推荐（按优先级）

#### 音效（SFX）推荐顺序

| 优先级 | 网站 | 网址 | 特点 | 授权 |
|--------|------|------|------|------|
| ⭐⭐⭐ | **Kenney.nl** | https://kenney.nl/assets?q=audio | 全部 CC0，免署名，最省心 | CC0 |
| ⭐⭐ | **Mixkit** | https://mixkit.co/free-sound-effects/ | 免费商用，无需署名 | Mixkit License |
| ⭐⭐ | **OpenGameArt** | https://opengameart.org | 专为游戏，可直接商用 | CC0/CC-BY |
| ⭐ | **Freesound** | https://freesound.org | 最大音效社区，种类齐全 | CC0/CC-BY（需看具体）|
| ⭐ | **Zapsplat** | https://zapsplat.com | 数量巨大，需注册 | 免费需署名/付费免署名 |

**新手最省心方案**：直接下 Kenney 的 "Interface Sounds" + "Impact Sounds" 两个包，能覆盖大部分 SFX 需求。

#### BGM 推荐顺序

| 优先级 | 网站 | 网址 | 特点 | 授权 |
|--------|------|------|------|------|
| ⭐⭐⭐ | **Pixabay Music** | https://pixabay.com/music/ | BGM 多，免署名 | Pixabay License |
| ⭐⭐ | **Kenney.nl** | https://kenney.nl/assets?q=music | CC0，但数量较少 | CC0 |
| ⭐ | **Free Music Archive** | https://freemusicarchive.org | 老牌，需筛选授权 | 看具体 |
| ⭐ | **Bensound** | https://www.bensound.com | 风格全，部分需署名 | 免费需署名/付费免 |

#### 卡牌图推荐顺序

| 优先级 | 网站 | 网址 | 特点 | 授权 |
|--------|------|------|------|------|
| ⭐⭐⭐ | **Leonardo.ai** | https://leonardo.ai | 游戏美术专用，免费额度 | AI 生成（看条款）|
| ⭐⭐ | **Stable Diffusion** | 本地部署 | 开源免费，可定制 | AI 生成 |
| ⭐⭐ | **Kenney.nl** | https://kenney.nl/assets | CC0 卡牌图标 | CC0 |
| ⭐ | **game-icons.net** | https://game-icons.net | 简洁图标，适合卡牌 | CC-BY（需署名）|
| ⭐ | **OpenGameArt** | https://opengameart.org | 2D 美术资源 | CC0/CC-BY |

#### 背景图推荐顺序

| 优先级 | 网站 | 网址 | 特点 | 授权 |
|--------|------|------|------|------|
| ⭐⭐⭐ | **Pixabay** | https://pixabay.com/images/ | 插画+照片，可做背景 | Pixabay License |
| ⭐⭐ | **Unsplash** | https://unsplash.com | 高质量照片 | Unsplash License |
| ⭐⭐ | **Leonardo.ai** | https://leonardo.ai | AI 生成奇幻场景 | AI 生成 |
| ⭐ | **OpenGameArt** | https://opengameart.org | 游戏背景 | CC0/CC-BY |

#### 版权注意事项

| 授权类型 | 含义 | 是否需署名 |
|----------|------|------------|
| **CC0** | 完全放弃版权，公共领域 | 否 |
| **CC-BY** | 可商用但需署名作者 | 是（在 credits 中列出）|
| **Mixkit/Pixabay License** | 平台自定义，通常可商用免署名 | 否 |

**建议**：优先选 CC0 素材，避免署名麻烦。游戏 credits 页面建议加上所有素材来源。

#### 格式转换工具

- **Audacity**（免费）：音频编辑 + 格式转换 + 音量归一化
- **在线转换器**：convertio.co（音频/图片格式转换）
- **GIMP** / **Krita**（免费）：图片编辑

---

## 三、T4：粒子效果 P1（AI 独立完成）

### 前置条件
T3 已完成（✅）。

### AI 负责的工作

1. **新建 `scripts/utils/particle_helpers.gd`**：
   - 封装 6 种粒子效果函数（参考 `美术开发大纲2.md` 第 2.3 节）
   - 函数：`spawn_hit_spark` / `spawn_death_dissolve` / `spawn_heal` / `spawn_block_shield` / `spawn_poison_loop` / `spawn_fire`

2. **修改 `scripts/ui/monster_slot.gd`**：
   - `play_hit_animation` 末尾调 `spawn_hit_spark`
   - `play_death_animation` 调 `spawn_death_dissolve`
   - 中毒状态变化时启动/停止 `spawn_poison_loop`（保存引用以便停止）

3. **修改 `scripts/ui/battle_screen.gd`**：
   - 玩家治疗事件调 `spawn_heal`
   - 玩家获得格挡事件调 `spawn_block_shield`
   - 烈焰术士攻击事件调 `spawn_fire`

4. **性能约束**：
   - 单次粒子数量 ≤ 30
   - 同时存活粒子总数 ≤ 100
   - 循环粒子在状态结束时必须 `queue_free`
   - `one_shot = true` 的粒子要连 `finished` 信号自动清理

5. **验证**：
   - `run_project` 无报错
   - 各粒子效果可见
   - 长时间战斗后粒子节点不堆积

### 用户需要补充
**无**。纯代码任务。

### 完成标志
- 6 种粒子效果都能触发
- 性能无明显下降
- 控制台无报错

### 给 AI 的提示词
```
请按 美术开发大纲2.md 的 T4 任务执行：
1. 新建 scripts/utils/particle_helpers.gd，封装 6 种粒子函数（参考大纲2 第 2.3 节）
2. 修改 monster_slot.gd 接入受击火星/死亡消散/中毒气泡
3. 修改 battle_screen.gd 接入治疗光效/格挡护盾/火焰
4. 性能约束：单次 ≤30 粒子，总数 ≤100，循环粒子要能停止
5. one_shot 粒子要连 finished 信号自动 queue_free
6. 每做一个粒子就 run_project 测一次，不要批量做

注意：先读 PROJECT_STATUS.md 和 美术开发大纲2.md 了解已完成任务，不要重做 T1/T3/T5/T6。
```

---

## 四、T8：背景图代码支持（AI 独立完成）

### 前置条件
无。

### AI 负责的工作

1. **修改 `data/encounters.json`**：
   - 为 BOSS 关添加 `"bg": "boss_bg"`
   - 为火堆关添加 `"bg": "campfire_bg"`
   - 其他关不填或填 `""`（用默认 `battle_bg`）

2. **修改 `scripts/ui/battle_screen.gd`**：
   - 读取关卡的 `bg` 字段
   - 加载 `res://assets/backgrounds/{bg}.png`
   - **关键**：文件不存在时用默认 `battle_bg.png`，不报错

3. **验证**：
   - `run_project` 无报错
   - 普通关显示默认背景
   - BOSS 关/火堆关无图时也显示默认背景（不崩溃）

### 用户需要补充
**无**。代码优雅处理文件缺失。

### 完成标志
- encounters.json 有 bg 字段
- battle_screen.gd 支持背景切换
- 无图时正常显示默认背景

### 给 AI 的提示词
```
请按 美术开发大纲2.md 的 T8 任务执行：
1. 修改 data/encounters.json，为 BOSS 关和火堆关添加 bg 字段
2. 修改 battle_screen.gd，根据关卡 bg 字段加载背景
3. 关键：背景图不存在时用默认 battle_bg，不报错（用 ResourceLoader.exists 检查）
4. 完成后 run_project 验证

注意：先读 PROJECT_STATUS.md 和 美术开发大纲2.md 了解已完成任务，不要重做 T1/T3/T5/T6。
此阶段没有实际背景图，代码要能优雅处理文件缺失。
```

---

## 五、T10：氛围动效 P2（AI 独立完成）

### 前置条件
T3 已完成（✅）。建议 T4 完成后再做（都改 monster_slot.gd）。

### AI 负责的工作

1. **在 `tween_helpers.gd` 新增 5 个函数**（参考 `美术开发大纲2.md` 第 4.3 节）：
   - `reward_card_popup(card)` - 奖励卡牌弹出
   - `intent_switch(label)` - 怪物意图切换
   - `campfire_flicker(node)` - 火堆火焰跳动（循环）
   - `monster_idle_breath(node)` - 怪物待机呼吸（循环）
   - `boss_entrance(boss_node)` - BOSS 登场动画

2. **修改 `scripts/ui/monster_slot.gd`**：
   - `_ready` 启动 `monster_idle_breath`（保存 Tween 引用）
   - 意图刷新时调 `intent_switch`
   - **死亡时 kill 待机呼吸 Tween**，避免与 dissolve 冲突

3. **修改 `scripts/ui/battle_screen.gd`**：
   - BOSS 关刷新怪物时，对 BOSS 调 `boss_entrance`
   - BOSS 登场期间禁用玩家输入

4. **修改 `scripts/ui/reward_modal.gd`**：
   - 卡牌创建后调 `reward_card_popup`

5. **修改 `scripts/ui/campfire_screen.gd`**：
   - `_ready` 时对火焰节点调 `campfire_flicker`

6. **验证**：
   - `run_project` 无报错
   - 各氛围动效可见
   - 长时间运行无内存泄漏（循环 Tween 不堆积）

### 用户需要补充
**无**。纯代码任务。

### 完成标志
- 5 个氛围动效都能看到
- 不影响战斗节奏
- 控制台无报错

### 给 AI 的提示词
```
请按 美术开发大纲2.md 的 T10 任务执行：
1. 在 tween_helpers.gd 添加 reward_card_popup / intent_switch / campfire_flicker / monster_idle_breath / boss_entrance
2. 在 monster_slot.gd 添加待机呼吸（_ready 启动，死亡时 kill）
3. 在 battle_screen.gd 添加 BOSS 登场动画（登场期间禁用输入）
4. 在 reward_modal.gd 接入奖励卡牌弹出
5. 在 campfire_screen.gd 接入火堆火焰跳动
6. 完成后 run_project 验证

只用 Tween，不要用 AnimationPlayer。
注意：先读 PROJECT_STATUS.md 和 美术开发大纲2.md 了解已完成任务，不要重做 T1/T3/T5/T6。
```

---

## 六、Bug2：F11 全屏修复（低优先级，顺手修）

### AI 负责的工作

1. **读 `scripts/main.gd`** 确认 `_input` 处理 KEY_F11 的逻辑
2. **读 `scripts/ui/battle_screen.gd`** 的 `_input`（约 790 行）确认是否抢先消费
3. **修复方案**（任选其一）：
   - 把 `main.gd` 的 `_input` 改成 `_unhandled_input`
   - 或在 `battle_screen.gd` 的 `_input` 末尾不消费 F11
4. **验证**：运行游戏按 F11，看控制台是否打印 `[Main] Switched to fullscreen mode`

### 给 AI 的提示词
```
请修复 Bug2：F11 全屏可能不生效。
1. 读 main.gd 确认 _input 处理 KEY_F11 的逻辑
2. 读 battle_screen.gd 的 _input 确认是否抢先消费
3. 尝试把 main.gd 的 _input 改成 _unhandled_input
4. run_project 验证 F11 是否生效

注意：先读 PROJECT_STATUS.md 第 8.2 节了解 Bug2 详情。
```

---

## 七、T2：音效素材接入（用户补素材后，AI 集成）

### 前置条件
用户已完成 T0，把 SFX 15 个文件放入 `assets/audio/sfx/`，BGM 5 首放入 `assets/audio/bgm/`。

### AI 负责的工作

1. **检查素材完整性**：
   - 遍历 `SFX_PATHS` / `BGM_PATHS`，用 `ResourceLoader.exists()` 检查每个文件
   - 列出缺失文件，告知用户

2. **BGM 循环配置**：
   - 检查 `.ogg` 文件的导入设置，确保 `loop` 开启
   - 修改 `.import` 文件或重新导入

3. **音量统一调整**：
   - 如果某些音效过响/过轻，调整 `_sfx_volume` 或单独音量
   - （AI 无法听，需用户反馈后调整）

4. **验证**：
   - `run_project` 启动
   - 用户实际触发各场景，确认音效播放
   - 用户反馈哪些音效需要调整音量/替换

### 用户需要补充
- **SFX 15 个**（见 T0 清单）
- **BGM 5 首**（见 T0 清单）
- 反馈音效听感（AI 调不了音量，需用户告知）

### 完成标志
- 所有音效能正常播放
- 音量统一（用户确认）
- BGM 能循环播放

### 给 AI 的提示词
```
请按 美术开发大纲2.md 的 T2 任务执行：
1. 检查 assets/audio/ 下所有音频文件是否齐全（对照 SFX_PATHS/BGM_PATHS）
2. 列出缺失文件
3. 检查 .ogg 的 loop 导入设置
4. 运行游戏，让我触发各场景测试音效
5. 根据我的反馈调整音量

我已准备的素材：[列出已放入的文件]

注意：先读 PROJECT_STATUS.md 和 美术开发大纲2.md 了解已完成任务，不要重做 T1。
T1 音效框架已完成，本任务只是填入实际音频文件。
```

---

## 八、T7：卡牌图素材接入（用户补图后，AI 集成）

### 前置条件
用户已准备卡牌图片，放入 `assets/cards/`，文件名与 `data/cards/` 下 JSON 文件名对应。

### AI 负责的工作

1. **检查图片完整性**：
   - 遍历 `data/cards/*.json`，检查对应图片是否存在
   - 列出缺失图片，告知用户

2. **填写 JSON 的 art 路径**：
   - 对每个有图的卡牌，填写 `"art": "res://assets/cards/xxx.png"`

3. **调整图片显示**：
   - 根据用户反馈调整 TextureRect 的尺寸/拉伸方式
   - 确保所有卡牌图风格统一

4. **验证**：
   - `run_project` 无报错
   - 所有有图的卡牌正常显示图片
   - 布局美观

### 用户需要补充
- **卡牌图片 20 张**（见 T0 清单）
- 反馈图片显示效果（AI 看不到实际渲染，需用户确认）

### 完成标志
- 所有卡牌显示图片
- 布局美观（用户确认）
- 控制台无报错

### 给 AI 的提示词
```
请按 美术开发大纲2.md 的 T7 任务执行：
1. 检查 assets/cards/ 下图片是否齐全（对照 data/cards/ 文件名）
2. 为每个 JSON 填写 art 路径（res://assets/cards/xxx.png）
3. 运行游戏让我看效果
4. 根据我的反馈调整图片显示

我已准备的卡牌图：[列出已放入的文件]

注意：先读 PROJECT_STATUS.md 和 美术开发大纲2.md 了解已完成任务，不要重做 T6。
T6 卡牌图代码支持已完成（TextureRect 已加，art 字段已存在），本任务只是填入实际图片路径。
```

---

## 九、T9：背景图素材接入（用户补图后，AI 集成）

### 前置条件
- 用户已准备背景图，放入 `assets/backgrounds/`
- T8 已完成（背景切换代码已就绪）

### AI 负责的工作

1. **检查图片完整性**：
   - 检查 `boss_bg.png` / `campfire_bg.png` 是否存在
   - 列出缺失图片

2. **调整背景显示**：
   - 根据用户反馈调整背景缩放/位置
   - 确保背景不遮挡 UI

3. **验证**：
   - `run_project` 无报错
   - BOSS 关显示 boss 背景
   - 火堆关显示火堆背景

### 用户需要补充
- **背景图 2 张**（见 T0 清单）
- 反馈显示效果

### 完成标志
- 不同关卡显示不同背景
- 背景不遮挡 UI
- 控制台无报错

### 给 AI 的提示词
```
请按 美术开发大纲2.md 的 T9 任务执行：
1. 检查 assets/backgrounds/ 下 boss_bg.png 和 campfire_bg.png 是否存在
2. 运行游戏让我看效果
3. 根据我的反馈调整背景显示（缩放/位置）
4. 确保背景不遮挡 UI

我已准备的背景图：[列出已放入的文件]

注意：先读 PROJECT_STATUS.md 和 美术开发大纲2.md 了解已完成任务。
T8 背景图代码支持应已完成，本任务只是填入实际图片并调整显示。
```

---

## 十、T11：最终整合测试（AI 独立完成）

### 前置条件
所有其他任务完成。

### AI 负责的工作

1. **全流程测试**：
   - 启动游戏 → 标题 → 战斗 → 火堆 → BOSS → 通关
   - 检查每个环节的音效/动效/粒子/着色器

2. **性能检查**：
   - 粒子总数是否超 100
   - Tween 是否有内存泄漏（未 kill 的循环动画）
   - 着色器是否影响帧率

3. **Bug 1 验证**：
   - 带荆棘护甲打怪，确认反伤弹数字 + 怪物死亡触发下一关

4. **Bug 2 验证**：
   - F11 全屏切换

5. **修复发现的问题**：
   - 动画冲突
   - 音效缺失
   - 着色器兼容性

6. **生成测试报告**：
   - 列出所有已实现功能
   - 列出已知问题
   - 列出待用户确认的事项

### 用户需要补充
- 反馈整体体验
- 指出需要调整的地方

### 完成标志
- 全流程无报错
- 性能 acceptable
- 用户确认体验 OK

### 给 AI 的提示词
```
请按 美术开发大纲2.md 的 T11 任务执行最终整合测试：
1. 全流程测试：启动 → 标题 → 战斗 → 火堆 → BOSS → 通关
2. 检查每个环节的音效/动效/粒子/着色器
3. 性能检查：粒子总数 ≤ 100，Tween 无泄漏，帧率稳定
4. Bug1 验证：带荆棘护甲打怪
5. Bug2 验证：F11 全屏
6. 生成测试报告：已实现功能 / 已知问题 / 待确认事项

注意：先读 PROJECT_STATUS.md 和 美术开发大纲2.md 了解全部任务状态。
```

---

## 十一、执行建议

### 给用户的建议

1. **并行进行**：
   - 你一边找素材（T0）
   - 一边让 AI 串行做纯代码任务（T4 → T8 → T10 → Bug2）

2. **AI 纯代码任务必须串行**：T4/T8/T10 都改 `battle_screen.gd`，不能并行

3. **每个任务开新对话**：避免上下文过长导致 AI 出错

4. **每完成一个任务就 commit**：便于回滚

5. **素材分批提供**：不必一次找齐，可以先做 SFX，再做卡牌图，最后做背景图

### 给 AI 的建议（每个任务开始时强调）

```
执行规则：
1. 严格按照任务描述执行，不要多做
2. 每改一个文件就测一次（run_project）
3. 遇到需要用户补充素材的地方，明确告知"需要 XX 素材"，不要跳过
4. 完成后列出：已改文件、已实现功能、待用户确认事项
5. 如果发现问题但不在本任务范围，记录但不修复
6. 先读 PROJECT_STATUS.md 和 美术开发大纲2.md，不要重做已完成任务
```

### 任务执行顺序推荐

**方案A：串行（稳妥，推荐）**
```
1. 用户开始找素材（T0，并行进行）
2. AI 做 T4（粒子）
3. AI 做 T8（背景图代码）
4. AI 做 T10（氛围动效）
5. AI 做 Bug2（F11，顺手）
--- 素材到位后 ---
6. AI 做 T2（音效接入）
7. AI 做 T7（卡牌图接入）
8. AI 做 T9（背景图接入）
--- 最后 ---
9. AI 做 T11（整合测试）
```

**方案B：并行（高效，需多开对话）**
```
用户准备素材（T0）

对话1（串行）：T4 → T8 → T10 → Bug2
对话2（等素材）：T2（音效接入）
对话3（等素材）：T7（卡牌图接入）
对话4（等素材 + T8完成）：T9（背景图接入）

最后：T11（整合，单开对话）
```

### 单次对话工作量参考

| 工作量 | 改动文件数 | 代码行数 | 适合任务 |
|--------|------------|----------|----------|
| 轻 | 1-3 个 | <50 行 | T2、T7、T8、T9、Bug2、T11 |
| 中 | 4-7 个 | 50-200 行 | T4、T10 |

**红线**：单次对话改动超过 8 个文件或 200 行代码，AI 出错概率显著上升，应拆分。

---

## 十二、风险与应对

| 风险 | 表现 | 应对 |
|------|------|------|
| AI 重做已完成任务 | 破坏 T1/T3/T5/T6 | 每个任务开始前强调"先读 PROJECT_STATUS.md" |
| AI 改错 battle_screen.gd | 战斗功能异常 | T4/T8/T10 串行，每个任务前 git commit |
| AI 上下文过长 | 后期遗忘前面的修改 | 每个任务开新对话，粘贴大纲2 |
| 素材风格不统一 | 视觉割裂 | 用户准备素材时注意统一风格 |
| 粒子性能问题 | 帧率下降 | 控制数量 ≤100，T11 整合时检查 |
| 动画冲突 | 卡牌闪烁/抖动/溶解被覆盖 | T10 时注意 kill 待机呼吸 Tween |
| 音效文件名不匹配 | load() 返回 null | 严格按 T0 清单的文件名命名 |
| BGM 不循环 | 播完就停 | T2 时检查 .ogg 的 loop 导入设置 |

---

## 十三、快速启动

复制以下内容到新对话开始一个任务：

```
我正在开发 Godot 卡牌游戏，项目路径：d:\Desktop\求职\个人网站开发\game-godot

请参考 PROJECT_STATUS.md、美术开发大纲2.md 和 任务编排2.md，执行任务 TX：[任务名称]

执行规则：
1. 严格按照任务描述执行，不要多做
2. 每改一个文件就测一次（run_project）
3. 遇到需要用户补充素材的地方，明确告知，不要跳过
4. 完成后列出：已改文件、已实现功能、待用户确认事项
5. 先读 PROJECT_STATUS.md 了解项目现状，不要重做已完成任务（T1/T3/T5/T6/Bug1）

[粘贴对应任务的"给 AI 的提示词"]
```

---

## 十四、文档关系说明

| 文档 | 用途 | 何时读 |
|------|------|--------|
| `PROJECT_STATUS.md` | 项目全貌 + 已完成任务详情 | 每个新对话必读 |
| `美术开发大纲.md` | 原始大纲（全部任务的技术细节）| 需要查阅已完成任务的技术细节时读 |
| `美术开发大纲2.md` | 剩余任务的技术细节 | 执行剩余任务时读 |
| `任务编排.md` | 原始编排（全部任务的执行计划）| 一般不需要读（已被 v2 取代）|
| `任务编排2.md`（本文档）| 剩余任务的执行编排 | 执行剩余任务时读 |

**推荐阅读顺序**：
1. `PROJECT_STATUS.md`（了解项目现状）
2. `美术开发大纲2.md`（了解剩余任务技术细节）
3. `任务编排2.md`（了解执行顺序和提示词）

---

*文档结束。新 AI 读完上述三个文档后，应能直接执行剩余任务。*
